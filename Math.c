#include<stdio.h>
#include<math.h>
int main () {
    double A = sqrt(9);
    printf("Square Root of 9 is %lf", A);
    double B = pow(2, 4);
    printf("\n2 to the power of 4 is %.0lf", B);
    return 0;
}